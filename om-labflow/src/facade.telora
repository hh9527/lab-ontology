# om-labflow 公共 facade（兼容性 re-export）
#
# 规范实现位于 `src/query.telora`（公共 typed/dynamic query facade）。本模块
# 保留 `om-labflow/facade` 模块 ID，只 re-export 同一组公共函数，避免破坏早期
# 引用。新代码应直接使用 `@src/query`。

import "@src/query" { build_from_request, build_from_value, encode_query, decode_request };

export { build_from_request, build_from_value, encode_query, decode_request };
